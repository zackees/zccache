# Compiler policy integration tests

`portable_policy.rs` exercises the public policy surface with or without native
features. Run with `soldr cargo test -p zccache-compiler --no-default-features --test portable_policy`.
