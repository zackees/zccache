# mtime_replay/tests

Unit tests for `snapshot`/`replay`/`replay_one`, covering the content-verified
restore decision table, exclude resolution, path-escaping rejection, and the
manifest JSON roundtrip.
