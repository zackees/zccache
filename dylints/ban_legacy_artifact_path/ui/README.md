# UI fixtures

`disallowed.rs` covers direct, split-statement, and concatenated flat-v1
artifact-name construction. `allowed.rs` represents calls into the shared
layout owner. The adjacent `.stderr` snapshot is the required diagnostic
contract.
