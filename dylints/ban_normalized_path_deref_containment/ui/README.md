# UI fixtures

`disallowed.rs` proves rustc's receiver adjustments are inspected correctly;
`allowed.rs` proves inherent normalized methods remain usable. The adjacent
`.stderr` snapshot is the required diagnostic contract.
